# Local development configuration. Docker/Kubernetes override these values with env vars.
spring.application.name=product-api

# Local MySQL datasource settings.
spring.datasource.url=jdbc:mysql://localhost:3306/productdb

spring.datasource.username=root

spring.datasource.password=root

# JDBC driver used by Spring Data JPA.
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate/JPA behavior.
spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=true

spring.jpa.properties.hibernate.format_sql=true

# App runtime settings.
server.port=8080

# JWT secret and token lifetime used for auth endpoints and request filtering.
app.jwt.secret=VGhpc0lzQVNlY3JldEtleUZvckp3dFRva2VuU2lnbmluZ1Rlc3QxMjM0NTY3ODkw
app.jwt.expiration-ms=3600000
