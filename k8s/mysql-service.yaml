apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: product-api
spec:
  # This stable service name is what the Spring Boot datasource URL resolves through cluster DNS.
  selector:
    app: mysql
  ports:
    - name: mysql
      protocol: TCP
      port: 3306
      targetPort: 3306
  type: ClusterIP
